import React from 'react'

function ParentList(props) {
  return (
    <div>
      {/* <li > {`person name:${props.name} age ${props.age} skill ${props.skill}`}</li> */}
      
      
    </div>
  )
}

export default ParentList
